name: panel_ui
description: Flutter web panel for intelligent warehouse management agent

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.5
  intl: ^0.17.0
  shared_preferences: ^2.2.2
  flutter_spinkit: ^5.2.0
  fl_chart: ^0.63.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
